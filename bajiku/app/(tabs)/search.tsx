
import React from 'react';
import { View, Text, StyleSheet, SafeAreaView, StatusBar } from 'react-native';
 
const styles = StyleSheet.create({
    container: {
        flex: 1
    }
});
 
const SearchScreen = () => {
    return (
        <SafeAreaView  className="mt-72">

        </SafeAreaView>
    );
}

 
export default SearchScreen;